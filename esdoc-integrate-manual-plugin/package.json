{
  "name": "esdocs-integrate-manual-plugin",
  "version": "1.2.0",
  "description": "A integrate manual plugin for ESDoc",
  "devDependencies": {
    "esdocs": "latest",
    "mocha": "2.5.3"
  },
  "engines": {
    "node": ">= 6.0.0"
  },
  "scripts": {
    "test": "rm -rf ./test/out && mocha --require ./test/init.js $(find test/ -regex '.*.test.js$') -R spec"
  },
  "keywords": [
    "esdocs",
    "manual",
    "plugin"
  ],
  "files": [
    "src",
    "README.md"
  ],
  "main": "src/Plugin.js"
}
